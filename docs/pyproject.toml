[project]
name = "openpfc-docs"
version = "0.1.0"
description = "MkDocs + Material toolchain for the prose site (see ../mkdocs.yml, docs/mkdocs_preview.md)"
requires-python = ">=3.12"
dependencies = [
    "mkdocs>=1.6.1",
    "mkdocs-material>=9.5",
]
